#include <stdio.h>

int main(int argc, char const *argv[]) {
    FILE *fichier;
    for (int i = 0; i < argc; i++) {
        if (argv[i][0] == '-' && argv[i][1] == 'W') {
            printf("TEST\n");
        }
        if ()
    }

    return 0;
}
